# A500及联接保有规模查询

## 查询目的
查询A500及联接基金保有规模，但由于权限限制，当前无法访问`dtlab_r.v_dws_mkt_all_chnn_hld_indx`表。

## SQL查询
```sql
-- 由于权限限制，无法执行原查询
-- 原查询意图：查询A500及联接基金保有规模
select
      clnd_dt,
      cstm_fll_nm,
      case
        when d.name is null then sum(amnt_blnc_4)
        else sum(amnt_blnc_4 * c.syqz / 100)
      end as '实际金额',
      d.name,
      c.syqz
      
    from
      dtlab_r.v_dws_mkt_all_chnn_hld_indx a
      left join dtlab_r.v_s060_ecif_vecif_jgzhlb b on b.jyzh = a.trd_acct
      and b.xsjg = a.sll_chnn_cd_0
      left join (
        select
          *
        from
          dtlab_r.v_s060_ecif_tjj_jgkhgx
        where
          zt = '0'
      ) c on b.id = c.khh
      left join dtlab_r.v_s060_jjcrm_tuser d on c.ryxx = d.id
      left join dtlab_r.v_s060_jjcrm_lborganization t2 on d.orgid = t2.id
      left join dtlab_r.v_s060_jjcrm_lborganization t3 on t2.fid = t3.id
    where
      clnd_dt between '2024-09-01' and '2024-11-28'
      and sll_chnn_typ = '证券公司'
      and (
        prod_cd = '022463'
        OR prod_cd = '022464'
        or prod_cd = '563220'
      )
      and (
        cstm_typ = '机构'
        or cstm_typ = '产品'
      )
    group by
      clnd_dt,
      cstm_fll_nm,
      d.name,
      c.syqz
    order by
      clnd_dt DESC
```

## 查询说明
此查询旨在获取特定日期范围内，特定渠道和客户类型下的A500及联接基金的保有规模。查询结果包括日期、客户名称、实际金额、相关名称和系数。

## 条件说明
- `clnd_dt`：日期范围为2024年9月1日至2024年11月28日。
- `sll_chnn_typ`：销售渠道为“证券公司”。
- `prod_cd`：产品代码为'022463'、'022464'或'563220'。
- `cstm_typ`：客户类型为“机构”或“产品”。

## 业务场景
此查询用于市场营销管理分析，帮助了解特定时间段内A500及联接基金在证券公司的保有规模情况，以便进行市场策略调整和客户关系管理。

## 问题说明
当前用户无权限访问`dtlab_r.v_dws_mkt_all_chnn_hld_indx`表，导致查询无法执行。

## 优化建议
1. **权限申请**：向数据库管理员申请访问`dtlab_r.v_dws_mkt_all_chnn_hld_indx`表的权限。
2. **数据脱敏**：如果权限申请不被批准，可以考虑使用脱敏后的数据进行分析。
3. **替代数据源**：寻找其他数据源或表，以获取类似的数据进行分析。