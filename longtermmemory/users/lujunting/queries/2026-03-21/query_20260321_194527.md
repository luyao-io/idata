# 有管理费收入未分配账户查询

## 查询目的
查询有管理费收入但未分配的账户信息。

## SQL查询
```sql
-- 由于权限限制，无法执行原查询
-- 原查询意图：查询有管理费收入但未分配的账户信息
select
  a.fnd_acct as '基金账号',
  sum(a.mngm_fee) as '管理费',
  b.zhmc as '账户名称'
from
  dtlab_r.v_dws_mkt_all_chnn_hld_indx a
  left join dtlab_r.v_s060_ecif_vecif_jgzhlb b on b.jyzh = a.trd_acct
  and b.xsjg = a.sll_chnn_cd_0
  left join dtlab_r.v_s060_ecif_tjj_jgkhkzxx_jgkh c on c.id = b.khgs
where
  a.clnd_dt between '2024-01-01' and '2024-08-12'
  and c.GSMC is null
  and a.mngm_fee is not null
  and a.mngm_fee <> 0
  and b.zhmc is not null
group by a.fnd_acct, b.zhmc
order by sum(a.mngm_fee) DESC
```

## 查询说明
此查询旨在从`dtlab_r.v_dws_mkt_all_chnn_hld_indx`表中提取有管理费收入但未分配的账户信息，并通过`dtlab_r.v_s060_ecif_vecif_jgzhlb`和`dtlab_r.v_s060_ecif_tjj_jgkhkzxx_jgkh`表进行关联，以获取账户名称和其他相关信息。但由于当前用户无权限访问`dtlab_r.v_dws_mkt_all_chnn_hld_indx`表，查询无法执行。

## 条件说明
- `a.clnd_dt between '2024-01-01' and '2024-08-12'`: 限定日期范围为2024年1月1日至2024年8月12日。
- `c.GSMC is null`: 确保账户未分配。
- `a.mngm_fee is not null and a.mngm_fee <> 0`: 确保账户有管理费收入。
- `b.zhmc is not null`: 确保账户名称不为空。

## 业务场景
在营销管理分析中，需要定期检查有管理费收入但未分配的账户，以便及时处理和优化账户管理策略。

## 问题说明
当前用户无权限访问`dtlab_r.v_dws_mkt_all_chnn_hld_indx`表，导致查询无法执行。建议联系数据库管理员或相关负责人申请必要的访问权限。

## 优化建议
1. **权限申请**: 联系数据库管理员或相关负责人，申请对`dtlab_r.v_dws_mkt_all_chnn_hld_indx`表的访问权限。
2. **查询优化**: 如果数据量较大，可以考虑使用索引优化查询性能，特别是在日期字段和关联字段上。
3. **定期审查**: 定期审查账户分配情况，确保所有有管理费收入的账户都能及时分配，避免遗漏。