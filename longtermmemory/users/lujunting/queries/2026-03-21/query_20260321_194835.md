# 券商业务部券商保有渠道实际保有查询

## 查询目的
查询券商业务部券商保有渠道的实际保有情况。

## SQL查询
```sql
-- 由于权限限制，无法执行原查询
-- 原查询意图：查询券商业务部券商保有渠道实际保有情况
select sum(amnt_blnc_4 * c.syqz) as '实际金额'
from dtlab_r.v_dws_mkt_all_chnn_hld_indx a
left join dtlab_r.v_s060_ecif_vecif_jgzhlb b on b.jyzh = a.trd_acct
and b.xsjg = a.sll_chnn_cd_0
left join (select * from dtlab_r.v_s060_ecif_tjj_jgkhgx where zt = '0') c on b.id = c.khh 
left join dtlab_r.v_s060_jjcrm_tuser d on c.ryxx = d.id
left join dtlab_r.v_s060_jjcrm_lborganization t2 on d.orgid = t2.id
left join dtlab_r.v_s060_jjcrm_lborganization t3 on t2.fid = t3.id
where cnfr_dt = '2023-10-31'
and sll_chnn_typ = '证券公司'
and (cstm_typ = '机构' or cstm_typ = '产品')
and t3.name = '券商业务部';
```

## 查询说明
此查询旨在计算券商业务部在特定日期（2023-10-31）通过证券公司渠道保有的实际金额。查询涉及多个表的联接，以获取交易账户、客户类型、组织关系等信息，并最终计算出实际保有金额。

## 条件说明
- `cnfr_dt = '2023-10-31'`：确认日期为2023年10月31日。
- `sll_chnn_typ = '证券公司'`：销售渠道类型为证券公司。
- `(cstm_typ = '机构' or cstm_typ = '产品')`：客户类型为机构或产品。
- `t3.name = '券商业务部'`：所属组织名称为券商业务部。

## 业务场景
此查询适用于市场营销管理分析中的券商业务部，用于监控和分析通过证券公司渠道保有的资金情况，帮助业务部门了解当前的资金保有状况，以便做出相应的市场策略调整。

## 问题说明
当前查询因权限限制无法访问`dtlab_r.v_dws_mkt_all_chnn_hld_indx`表，导致查询无法正常执行。

## 优化建议
1. **权限申请**：向数据库管理员申请访问`dtlab_r.v_dws_mkt_all_chnn_hld_indx`表的权限。
2. **数据脱敏**：如果权限申请不被批准，可以考虑使用脱敏后的数据进行分析，确保数据安全的同时满足业务需求。
3. **替代方案**：寻找其他可访问的数据源或表，尝试构建类似的查询逻辑以达到相同的目的。