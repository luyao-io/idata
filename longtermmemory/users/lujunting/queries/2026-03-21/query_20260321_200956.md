# 场内年金投管人识别（最新版）查询

## 查询目的
查询场内年金投管人的相关信息，包括账户名称、交易账户、开户公司、CRM客户名称、询价机构名称、询价日期和结算会员名称。

## SQL查询
```sql
-- 由于权限限制，无法执行原查询
-- 原查询意图：场内年金投管人识别（最新版）
select
  b.ZHMC as '账户名称',
  b.jyzh,
  b.khgs,
  c.GSMC as 'CRM客户名称',
  e.inquirer as '询价机构名称',
  e.ann_dt as '询价日期',
  f.sttl_mmbr as '结算会员名称'
from
  dtlab_r.v_s060_ecif_vecif_jgzhlb b
  left join dtlab_r.v_s060_ecif_tjj_jgkhkzxx_jgkh c on c.id = b.khgs
  left join (
    select distinct
      inquirer_typecode,
      d.s_holder_type,
      inquirer,
      issuetarget,
      security_account,
      ann_dt
    from
      dtlab_r.v_s034_wfdb_ipoinquirydetails a
      left join (
        select distinct
          s_holder_typecode,
          s_holder_type
        from
          dtlab_r.v_s034_wfdb_ashareplacementdetails
      ) d on a.inquirer_typecode = d.s_holder_typecode
    where
      d.s_holder_type <> '个人投资者'
  ) e on e.security_account = b.jyzh
  left join (
    select distinct
      fnd_acct,
      sttl_mmbr
    from
      dtlab_r.v_dwd_mkt_etf_ta_hldr_rstr
  ) f on b.jyzh = f.fnd_acct
where
  b.zhmc like '%年金%'
```

## 查询说明
此查询旨在从多个视图中提取与场内年金投管人相关的详细信息。由于当前用户没有访问 `dtlab_r.v_s060_ecif_vecif_jgzhlb` 表的权限，导致查询无法执行。

## 条件说明
- `b.zhmc like '%年金%'`: 筛选账户名称中包含“年金”的记录。
- `d.s_holder_type <> '个人投资者'`: 排除个人投资者类型的记录。

## 业务场景
在营销管理分析中，需要识别并获取场内年金投管人的详细信息，以便进行进一步的市场分析和管理决策。

## 问题说明
当前用户没有访问 `dtlab_r.v_s060_ecif_vecif_jgzhlb` 表的权限，导致查询无法正常执行。建议联系数据库管理员或相关负责人申请必要的访问权限。

## 优化建议
1. **权限申请**：联系数据库管理员或相关负责人，申请对 `dtlab_r.v_s060_ecif_vecif_jgzhlb` 表的访问权限。
2. **查询优化**：如果数据量较大，可以考虑使用索引优化查询性能，或者分批次查询以减少单次查询的数据量。
3. **数据脱敏**：确保查询结果中的敏感信息已进行适当的脱敏处理，符合数据安全和隐私保护的要求。