# 渠道角度查询失败

## 查询目的
从渠道角度分析账户信息，但由于权限限制，无法访问`dtlab_r.v_s060_jjcrm_lborganization`表。

## SQL查询
```sql
-- 由于权限限制，无法执行原查询
-- 原查询意图：从渠道角度分析账户信息
select
  b.zhmc as '账户名称',
  a.fnd_acct as '基金账户',
  a.cstm_typ as '客户类型',
  case when b.zhmc is null then '渠道账户'
  else '机构账户' end as '根据账户名称判断',
  case when a.crm_bsns_typ = '渠道' then '渠道账户'
  else '机构账户' end as '根据业务类型判断',
  CASE WHEN (case when b.zhmc is null then '渠道账户'
  else '机构账户' end) = (case when a.crm_bsns_typ = '渠道' then '渠道账户'
  else '机构账户' end)  THEN '正确'
    ELSE '有误'
  END AS '判断账户划分机构渠道是否正确',
  c.gsmc as '公司名称',
  t2.name as '销售',
  t3.name as '团队',
  t4.name as '部门',
  case when c.gsmc is null and t2.name is not null then  '有销售无公司'
       when c.gsmc is not null and  t2.name is null then '有公司无销售'
       else '正常'
   end as '判断公司与销售是否对应',
   a.chnn_abbr as '渠道',
     case when t1.syqz is null then 1
  else t1.syqz/100 end as '收益权重',
  sum(a.tdy_hld_amnt*(case when t1.syqz is null then 1
  else t1.syqz/100 end)) / (DATEDIFF('day', '2024-01-01'::DATE, '2024-12-31'::DATE) + 1)  as '日均保有金额'
from
  dtlab_r.v_t_orgn_cstm_prft_dat a
 left join dtlab_r.v_s060_crm_r_vecif_jgzhlb b on b.jyzh = a.trd_acct  and b.xsjg=a.chnn_cd
 left join dtlab_r.v_s060_ecif_tjj_jgkhgx t1 on b.id =t1.khh and t1.zt='0'
 left join dtlab_r.v_s060_ecif_tjj_jgkhkzxx_jgkh c on c.id = b.khgs
 left join dtlab_r.v_s060_jjcrm_tuser t2 on t1.ryxx = t2.id
 left join dtlab_r.v_s060_jjcrm_lborganization t3 on t2.orgid = t3.id 
 left join dtlab_r.v_s060_jjcrm_lborganization t4 on t3.fid = t4.id
 where a.clnd_dt between '2024-01-01' and '2024-12-31'
group by 
a.crm_bsns_typ,b.zhmc,a.crm_cstm_nm,a.cstm_typ,c.gsmc,
t2.name,t1.syqz,a.chnn_abbr,t3.name,t4.name,a.fnd_acct
```

## 查询说明
此查询旨在从渠道角度分析账户信息，包括账户名称、基金账户、客户类型等，并计算日均保有金额。但由于当前用户无权限访问`dtlab_r.v_s060_jjcrm_lborganization`表，导致查询无法执行。

## 条件说明
- `a.clnd_dt between '2024-01-01' and '2024-12-31'`: 查询日期范围为2024年1月1日至2024年12月31日。
- `left join`多个表以获取相关联的数据，如账户信息、公司信息、销售信息等。
- `sum(a.tdy_hld_amnt * (case when t1.syqz is null then 1 else t1.syqz/100 end)) / (DATEDIFF('day', '2024-01-01'::DATE, '2024-12-31'::DATE) + 1)`: 计算日均保有金额。

## 业务场景
此查询适用于市场营销管理分析，帮助了解不同渠道和机构的账户情况及业绩表现。

## 问题说明
当前用户无权限访问`dtlab_r.v_s060_jjcrm_lborganization`表，导致查询无法完整执行。建议联系数据库管理员或数据所有者申请必要的访问权限。

## 优化建议
1. **权限申请**: 联系数据库管理员或数据所有者，申请访问`dtlab_r.v_s060_jjcrm_lborganization`表的权限。
2. **查询优化**: 如果权限无法立即获得，可以考虑使用其他替代数据源或简化查询逻辑，以部分实现查询目的。
3. **数据安全**: 确保在申请权限时明确使用目的，遵守数据安全和隐私政策。