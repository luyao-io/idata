# 查询盈亏数据落表

## 查询目的
查询特定日期范围内，产品类型为“公募”的盈亏数据，并计算加权保有金额。由于权限限制，无法访问`dtlab_r.v_s060_jjcrm_lborganization`表。

## SQL查询
```sql
-- 由于权限限制，无法执行原查询
-- 原查询意图：盈亏数据需要落表的
select
      a.crm_cstm_nm,
      a.prod_abbr,
      a.prod_cd,
            case when t4.name='CMO'  then t3.name
      else t4.name end as '部门',
       case
    when t1.syqz is null then 1
    else t1.syqz / 100
  end as '收益权重',
  t2.name,
      a.prod_typ_200272,
     sum(a.tdy_hld_amnt*(case
    when t1.syqz is null then 1
    else t1.syqz / 100
  end)) as '加权保有金额'
    from
      dtlab_r.v_t_orgn_cstm_prft_dat a 
       left join dtlab_r.v_s060_crm_r_vecif_jgzhlb u on u.jyzh = a.trd_acct
  and u.xsjg = a.chnn_cd
  left join dtlab_r.v_s060_ecif_tjj_jgkhgx t1 on u.id = t1.khh
  and t1.zt = '0'
  left join dtlab_r.v_s060_jjcrm_tuser t2 on t1.ryxx = t2.id
  left join dtlab_r.v_s060_jjcrm_lborganization t3 on t2.orgid = t3.id
  left join dtlab_r.v_s060_jjcrm_lborganization t4 on t3.fid = t4.id 
    where
      a.clnd_dt between  '2024-01-01'  and '2024-12-31'
      and a.prod_typ_203427 = '公募'
    group by
      a.clnd_dt,
      a.prod_abbr,
      a.prod_cd,
      a.prod_typ_200272,
      t3.name,
      t4.name,
      t1.syqz,
      t2.name,
      a.crm_cstm_nm
```

## 查询说明
此查询旨在从多个视图中提取客户、产品和交易相关数据，计算每个客户的加权保有金额，并按日期、产品简称、产品代码、产品类型等进行分组汇总。由于权限限制，部分组织结构信息可能无法获取。

## 条件说明
- `a.clnd_dt between '2024-01-01' and '2024-12-31'`: 查询日期范围为2024年1月1日至2024年12月31日。
- `a.prod_typ_203427 = '公募'`: 仅查询产品类型为“公募”的记录。

## 业务场景
此查询用于营销管理分析，帮助了解特定时间段内公募产品的盈亏情况及客户持有情况，以便制定更有效的营销策略。

## 问题说明
当前查询因权限限制无法访问`dtlab_r.v_s060_jjcrm_lborganization`表，导致部分组织结构信息无法获取。建议联系数据库管理员或系统负责人申请必要的访问权限。

## 优化建议
1. **权限申请**: 联系数据库管理员或系统负责人，申请对`dtlab_r.v_s060_jjcrm_lborganization`表的访问权限。
2. **查询优化**: 如果权限无法立即获得，可以考虑使用其他方式获取组织结构信息，例如通过API调用或其他数据源。
3. **性能优化**: 检查查询中的连接条件和过滤条件，确保查询效率。可以考虑创建索引以提高查询速度。