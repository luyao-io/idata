# 查询11失败

## 查询目的
查询产品基金经理与规模信息。

## SQL查询
```sql
-- 由于表名问题，无法执行原查询
-- 原查询意图：查询产品基金经理与规模信息
select
  t1.自然日期,
  t1.账户名称,
  ko.代码,
  ko.基金账号,
  ko.固收基金经理,
  ko.权益基金经理,
  t1.产品简称,
  t1.产品代码,
  t1.基金账号 as '公募的基金账号',
  sum(t1.当日规模) as '当日规模',
  case
    when t1.自然日期 = date ('20250522') then t1.当日规模
    else 0
  end as '期末规模',
  sum(t1.当日流入) as '当日流入',
  sum(t1.当日流出) as '当日流出',
  sum(t1.当日净流入) as '净流入',
  sum(t1.当日盈亏) as '当日盈亏'
from
  (
    SELECT
      代码,
      基金账号,
      LISTAGG (固收经理, ',') WITHIN GROUP (
        ORDER BY
          固收经理
      ) AS 固收基金经理,
       LISTAGG (权益经理, ',') WITHIN GROUP (
        ORDER BY
          权益经理
      ) AS 权益基金经理
    FROM
      (
        select distinct
          产品名称 as 代码,
          产品类型,
          产品主联系固收经理 as 固收经理,
          产品主联系权益经理 as 权益经理,
          SUBSTRING(产品账号, 1, POSITION('+' IN 产品账号) - 1) AS 基金账号
        from
          dtlab_w.baoyaoyao_private_fund_invest_info
        where
          产品类型 not in ('一对一', '一对多', '投顾')
          and 产品账号 is not null
        union
        select distinct
          产品名称 as 代码,
          产品类型,
          投资经理 as 固收经理,
          投资经理 as 权益经理,
          SUBSTRING(产品账号, 1, POSITION('+' IN 产品账号) - 1) AS 基金账号
        from
          dtlab_w.baoyaoyao_private_fund_invest_info
        where
          产品类型 in ('一对一', '一对多')
          and 产品账号 is not null
      ) t
    GROUP BY
      代码,
      基金账号
  ) ko
  left join (
    select
      clnd_dt as '自然日期',
      cstm_nm as '账户名称',
      prod_abbr as '产品简称',
      prod_cd as '产品代码',
      fnd_acct as '基金账号',
      sum(tdy_hld_amnt) as '当日规模',
      sum(tdy_infl) as '当日流入',
      sum(tdy_otfl) as '当日流出',
      sum(tdy_infl - tdy_otfl) as '当日净流入',
      sum(tdy_prft_lss) as '当日盈亏'
    from
      dtlab_r.v_t_orgn_cstm_prft_dat
    where
      clnd_dt between '20250522' and '20250522'
      and cstm_typ='产品'
    group by
      clnd_dt,
      fnd_acct,
      cstm_nm,
      prod_abbr,
      prod_cd
  ) t1 on t1.基金账号 = ko.基金账号
where
  t1.基金账号 is not null
group by
  t1.自然日期,
  t1.账户名称,
  ko.代码,
  ko.基金账号,
  ko.固收基金经理,
  ko.权益基金经理,
  t1.产品简称,
  t1.产品代码,
  t1.基金账号,
  t1.当日规模
```

## 查询说明
此查询旨在从`dtlab_w.baoyaoyao_private_fund_invest_info`和`dtlab_r.v_t_orgn_cstm_prft_dat`两个表中提取产品的基金经理信息及规模数据。查询结果包括自然日期、账户名称、基金代码、基金账号、固收基金经理、权益基金经理、产品简称、产品代码、当日规模、期末规模、当日流入、当日流出、净流入和当日盈亏等字段。

## 条件说明
- `产品类型`不为“一对一”、“一对多”或“投顾”，且`产品账号`不为空。
- `产品类型`为“一对一”或“一对多”，且`产品账号`不为空。
- `clnd_dt`在2025年5月22日之间，且`cstm_typ`为“产品”。

## 业务场景
此查询用于营销管理分析，帮助了解特定日期内各产品的基金经理及其管理规模的变化情况，以便进行更有效的市场分析和决策支持。

## 问题说明
当前查询因表名`dtlab_w.baoyaoy_private_fund_invest_info`可能存在拼写错误或不存在而无法执行。建议检查表名是否正确，并确认该表是否存在。

## 优化建议
1. **表名验证**：确保表名`dtlab_w.baoyaoy_private_fund_invest_info`正确无误，并且该表存在于数据库中。
2. **性能优化**：考虑对大表进行索引优化，特别是对`产品账号`、`产品类型`、`clnd_dt`等常用过滤条件字段建立索引。
3. **查询简化**：如果查询结果集较大，可以考虑分批处理或使用临时表来提高查询效率。
4. **数据清洗**：确保数据的一致性和完整性，特别是在处理`产品账号`和`产品类型`等关键字段时。