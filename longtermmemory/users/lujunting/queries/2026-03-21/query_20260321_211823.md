# 年金账户识别查询

## 查询目的
通过近似交易识别年金账户，但由于权限限制，无法访问`dtlab_r.v_s060_ecif_vecif_jgzhlb`表。

## SQL查询
```sql
-- 由于权限限制，无法执行原查询
-- 原查询意图：通过近似交易识别年金账户
select distinct
  d.fnd_acct_srrg_key,
  d.fnd_acct,
  d.count,
  c1.gsmc
from
  (
    select
      fnd_acct_srrg_key,
      fnd_acct,
      count(cnfr_dt) as count
    from
      (
        select
          a1.fnd_acct_srrg_key,
          a.cnfr_dt,
          a.fnd_acct,
          a.mn_prod_nm,
          a.fnd_bsns_typ
        from
          dtlab_r.v_dws_mkt_all_chnn_trd_indx a
          left join dtlab_r.v_dws_mkt_all_chnn_trd_indx a1 on a.prod_srrg_key = a1.prod_srrg_key
          and a.cnfr_dt = a1.cnfr_dt
          and a.fnd_bsns_typ = a1.fnd_bsns_typ
          and a1.fnd_acct in ('0899058791', '0899359313')
        where
          a.fnd_acct_srrg_key in (
            select distinct
              fnd_acct_srrg_key
            from
              dtlab_r.v_dws_mkt_all_chnn_trd_indx a
            where
              cstm_fll_nm ilike '%年金%'
              and cnfr_dt between '20220101' and '20241126'
              and (cnfr_dt, prod_srrg_key, fnd_bsns_typ) in (
                select
                  cnfr_dt,
                  prod_srrg_key,
                  fnd_bsns_typ
                from
                  dtlab_r.v_dws_mkt_all_chnn_trd_indx
                where
                  fnd_acct in ('0899058791', '0899359313')
                  and cnfr_dt between '20220101' and '20241126'
              )
          )
          and a.cnfr_dt between '20220101' and '20241126'
          and a1.cnfr_dt between '20220101' and '20241126'
      ) c
    group by
      fnd_acct_srrg_key,
      fnd_acct
  ) d
  left join dtlab_r.v_s060_ecif_vecif_jgzhlb b1 on b1.jyzh = d.fnd_acct
  left join dtlab_r.v_s060_ecif_tjj_jgkhkzxx_jgkh c1 on c1.id = b1.khgs
order by
  d.count DESC
```

## 查询说明
此查询旨在通过特定条件筛选出可能的年金账户，并统计这些账户的交易次数。然而，由于当前用户没有访问`dtlab_r.v_s060_ecif_vecif_jgzhlb`表的权限，导致查询无法完整执行。

## 条件说明
- `fnd_acct_srrg_key` 和 `fnd_acct`：用于标识基金账户。
- `cnfr_dt`：确认日期，范围为2022年1月1日至2024年11月26日。
- `cstm_fll_nm`：客户全名，使用`ilike '%年金%'`来匹配包含“年金”的账户。
- `prod_srrg_key` 和 `fnd_bsns_typ`：产品注册键和基金业务类型，用于关联交易记录。
- `fnd_acct in ('0899058791', '0899359313')`：指定的基金账户，用于进一步筛选。

## 业务场景
在营销管理分析中，识别潜在的年金账户对于制定针对性的营销策略至关重要。此查询帮助分析师从大量交易数据中筛选出符合特定条件的年金账户，以便进行更深入的分析和客户关系管理。

## 问题说明
当前用户没有访问`dtlab_r.v_s060_ecif_vecif_jgzhlb`表的权限，导致查询无法完整执行。建议联系数据库管理员或相关负责人申请必要的访问权限。

## 优化建议
1. **权限申请**：联系数据库管理员或相关负责人，申请访问`dtlab_r.v_s060_ecif_vecif_jgzhlb`表的权限。
2. **查询优化**：如果权限问题短期内无法解决，可以考虑使用其他表或字段来替代`dtlab_r.v_s060_ecif_vecif_jgzhlb`表中的数据，以完成查询目标。
3. **性能优化**：对于复杂的嵌套查询，可以考虑使用临时表或视图来简化查询逻辑，提高查询性能。