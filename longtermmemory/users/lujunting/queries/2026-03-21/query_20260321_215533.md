# CRM账户分配错误核对查询

## 查询目的
核对CRM系统中账户分配的错误情况，特别是针对客户编码（khbm）不一致的情况进行检查。

## SQL查询
```sql
-- 由于权限限制，无法执行原查询
-- 原查询意图：核对CRM账户分配错误
with
  tmp as (
    select
      t2.cpspv,
      t1.jjzh,
      t1.khbm,
      t1.khgs,
      c.gsmc
    from
      dtlab_r.v_s060_crm_r_vecif_jgzhlb t1
      left join dtlab_w.recorddata t2 on t1.jjzh = t2.jjzh
      left join dtlab_r.v_s060_ecif_tjj_jgkhkzxx_jgkh c on t1.khgs = c.id
    where
      t2.cpspv is not null
  ),
  tmp1 as (
    select
      t1.cpspv,
      t1.jjzh as 'jjzh1',
      t2.jjzh as 'jjzh2',
      t1.khbm as 'khbm1',
      t2.khbm as 'khbm2',
      t1.khgs,
      t1.gsmc
    from
      tmp t1
      join tmp t2 on t1.cpspv = t2.cpspv
      and t1.khbm <> t2.khbm
      and t1.khbm is not null
      and t2.khbm is not null
  )
select distinct
  cpspv,
  jjzh1,
  khgs,
  gsmc,
  khbm1,
  bmqc,
  bmjc
from
  tmp1 t1
  left join dtlab_r.v_s060_ecif_tjj_jgkhbmxx t2 on t1.khbm1 = t2.id
order by
  cpspv
```

## 查询说明
此查询旨在通过连接多个表来识别CRM系统中账户分配的错误。具体来说，它通过比较不同记录中的客户编码（khbm）来找出不一致的情况，并列出相关的账户信息和公司名称等。

## 条件说明
- `t2.cpspv is not null`: 确保cpspv字段不为空。
- `t1.khbm <> t2.khbm`: 确保两个记录的客户编码不同。
- `t1.khbm is not null` 和 `t2.khbm is not null`: 确保参与比较的客户编码均不为空。

## 业务场景
在营销管理分析中，确保CRM系统的账户分配准确无误是非常重要的。此查询帮助管理人员快速发现并解决账户分配中的错误，提高数据质量和客户服务质量。

## 问题说明
当前用户报告称无法访问`dtlab_r.v_s060_ecif_tjj_jgkhbmxx`表，导致查询失败。这可能是由于权限设置不当或数据库访问控制策略的原因。

## 优化建议
1. **权限调整**：联系数据库管理员，请求为执行此查询的用户授予必要的表访问权限。
2. **查询优化**：如果数据量较大，考虑使用索引优化查询性能，特别是在频繁用于连接操作的字段上创建索引。
3. **数据治理**：定期审查和更新数据访问权限，确保只有授权用户能够访问敏感数据，同时保证业务流程的顺畅运行。